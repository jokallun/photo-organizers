[tool.black]
line-length = 100
skip-string-normalization = true
exclude = '''
/(
    \.git
  | \.tox
  | \.venv
  | venv
  | \.env
  | env
  | _build
  | build
  | dist
  # The following are specific to Black, you probably don't want those.
  | blib2to3
  | tests/data
)/
'''
